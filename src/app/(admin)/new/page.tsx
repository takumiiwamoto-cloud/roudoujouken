/**
 * O-03 依頼新規作成画面(事務所向け)
 * Day 9-10(プロンプト4-B)で実装予定。会社情報入力 → URL 発行 → クリップボードコピー。
 */
export default function AdminNewRequestPage() {
  return (
    <main className="mx-auto max-w-2xl p-8">
      <h1 className="text-2xl font-bold">依頼新規作成(O-03)</h1>
      <p className="mt-4 text-muted-foreground">
        工事中: 会社情報入力 → URL 発行 → コピー機能を Day 9-10 で実装します。
      </p>
    </main>
  );
}
