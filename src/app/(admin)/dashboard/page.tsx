/**
 * O-02 依頼一覧画面(事務所向け)
 * Day 9-10(プロンプト4-B)で実装予定。ステータス別フィルタ付き一覧。
 */
export default function AdminDashboardPage() {
  return (
    <main className="mx-auto max-w-5xl p-8">
      <h1 className="text-2xl font-bold">依頼一覧(O-02)</h1>
      <p className="mt-4 text-muted-foreground">
        工事中: 依頼一覧(ステータス別フィルタ付き)を Day 9-10 で実装します。
      </p>
    </main>
  );
}
